# Anthropic API key — required for the live agent (monitoring cycle + chat)
# in app.py. Without it, the dashboard still runs but shows "Agent
# unavailable" instead of real decisions/answers. Not needed for
# mcp_server.py, which only exposes the deterministic tools (no LLM calls).
ANTHROPIC_API_KEY=sk-ant-your-key-here
